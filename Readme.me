# Criando um ambiente virtual (venv)
- python -m venv venv

# Ativar ambiente virtual (venv)
- .\venv\Scripts\activate


# Iniciar um repositorio GitHub
- git init
- git add .
- git commit -m "Initial project setup with venv and gitignore"